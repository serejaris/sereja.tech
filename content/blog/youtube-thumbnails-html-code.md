---
title: "YouTube-обложки без Canva и Figma"
date: 2026-02-06
description: "Как генерировать обложки для видео через HTML-шаблоны, Playwright и YouTube API. Без графических редакторов."
tags: ["youtube", "playwright", "вайбкодинг", "обложки", "claude-code"]
image: "/images/blog/youtube-thumbnails-html-code-preview.png"
---

Каждую неделю новый урок. Каждому уроку — обложка. Ни графический редактор, ни AI-генератор изображений не нужны — LLM сама делает обложку по одному промпту. Вот как это устроено на моём [канале](https://youtube.com/@serejaris).

Сделал через код.

## Проблема

Обложки для YouTube — это время. Загрузить шаблон в Canva, поменять текст, выровнять, экспортировать. Минут пятнадцать на одну картинку. А когда делаешь это раз в неделю для курса — три часа в месяц на чистую рутину.

Каждый раз одно и то же. Меняется номер недели и заголовок. Шрифт, цвета, сетка на фоне — всё идентично.

Хотел так: одна команда агенту — обложка готова и на YouTube.

## Решение

Попросил Claude Code на Opus создать HTML-шаблон для обложек:

{{< callout type="insight" >}}
Сделай HTML-файл для обложек уроков. Размер 1280×720. Стиль: тёмный фон #0c0c0c, grid-pattern на заднике, заголовок Space Grotesk 96px, детали JetBrains Mono, cyan акценты #0ed3cf. Потом заскриншотим через Playwright.
{{< /callout >}}

Агент собрал страницу, добавил safe zone для оверлея длительности (YouTube кладёт его в правый нижний угол), настроил типографику. Потом — второй промпт:

{{< callout type="insight" >}}
Открой HTML локально через Playwright, сделай скриншот 1280×720 и загрузи на YouTube через API. Токен обнови по refresh token.
{{< /callout >}}

Playwright рендерит HTML в PNG, curl отправляет на [YouTube API](https://developers.google.com/youtube/v3/docs/thumbnails/set) с OAuth. Две команды — обложка обновлена.

## Воркфлоу

```
HTML шаблон   Playwright    YouTube API
  (текст)  →  (скриншот) →  (загрузка)
     ↓             ↓             ↓
  редактор     1280×720       OAuth
  в Claude      PNG        refresh token
```

Теперь говорю агенту "обнови обложку, заголовок такой-то" — готово за секунды.

## Почему HTML, а не Figma

Перед тем как делать, посмотрел что работает. По [данным Awisee](https://awisee.com/blog/youtube-thumbnail-best-practices/), 90% топовых видео используют custom thumbnails, а лица с эмоциями дают +20-30% к CTR. [Alici.AI](https://alici.ai/blog/how-to-make-youtube-thumbnails-best-practices-2026) пишут что текст из 3-5 слов добавляет 30% к кликабельности, а visual cues вроде стрелок — ещё 25%.

Шрифт от 75px, чтобы читался на мобильных. По [vidIQ](https://vidiq.com/blog/post/youtube-thumbnail-size-measurements/) — минимальный размер 1280×720, формат PNG для текстовых обложек.

HTML даёт всё это. Типографика через CSS. Размеры в пикселях. Grid-pattern через background-image. Текст редактируется в одну строку — не нужно искать слой в Figma.

Похожий подход я уже использовал для [генерации карточек через HTML и Playwright](/blog/carousel-cards-playwright/) — та же механика. Там были квадратные карточки для Telegram, здесь — 16:9 обложки для видео.

## Детали

Playwright рендерит HTML за секунду — быстрее чем Canva грузится в браузере. [YouTube API](https://developers.google.com/youtube/v3/docs/thumbnails/set) принимает POST на `/thumbnails/set`, максимум 2MB. OAuth обновляется автоматически через refresh token, логиниться каждый раз не нужно.

[David Bushell](https://dbushell.com/2024/11/15/generate-open-graph-images-with-playwright/) делает так же для OG-изображений — HTML плюс Playwright. Увидел его статью и понял, что подход рабочий. У него — блог, у меня — обложки курса.

## Результат

Девять потоков "Кружка вайбкодинга". Четыре урока на поток. Тридцать шесть обложек.

Раньше — три часа рутины. Теперь обновляю текст в HTML и даю одну команду агенту.

Код в git. Откатиться к прошлой версии — одна команда. Поменять цвет акцента на всех обложках — одна строка в CSS. Не замена дизайнеру для сложных креативов, но для серийных контентных обложек — по-моему, лучший воркфлоу.
